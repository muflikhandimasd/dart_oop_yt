import 'person.dart';
import 'student.dart';

void main() {
  // Person() -> Memanggil constructor dari Person
  // Person p = Person();
  // Person p2 = Person(name: 'Muf');
  Person s = Student();

  // p.name = 'Muflikhan';

  // print(p.name);
  // print(p2.name);
  print(s.name);
}
